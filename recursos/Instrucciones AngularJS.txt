-- Para acceder a una carpeta
  cd m

-- Para crear un nuecvo componente
  ng generate component login
  ng g c login

-- Para crear un servicio
  ng generate service nombreServicio


-- INSTRUCCIONES PRUEBAS UNITARIAS -- 

  -- lanza servidor karma, por defecto de angular
  npm test
  -- Mantiene activo
  ng test

  -- Solo se ejecuta una vez
  ng test --single-run

  -- reporte coverage (Crea una carpta coverage donde se podra ver el reporte)
  ng test --code-coverage

  -- ruta para ver los reportes coverage de las pruebas
  file:///C:/Users/stive/Inmobiliaria/coverage/src/app/Paginas/publico/ver-inmueble/index.html